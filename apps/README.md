# apps/

Applications built on the ad toolkit.

## vim/

The vim application (`ad_vim`) — the primary consumer of the toolkit.
Launches vim with the animation engine, rendering the diff as if typed
by a human.

See [vim/README.md](vim/README.md) for details.
