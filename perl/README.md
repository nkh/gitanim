# perl/

Shared Perl library code. Currently empty — Perl twins for layers live
in `layers/perl/`, Perl twins for the diff engine live in
`diff_engine/perl/`, Perl twins for the animator live in
`animator/perl/`. This directory is reserved for cross-cutting Perl
modules (e.g. a future `ad::common` namespace).
